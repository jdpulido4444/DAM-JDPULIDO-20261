import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        marginBottom: 12,
        width: '100%',
    },
    label: {
        fontSize: 11,
        fontWeight: 'bold',
        color: '#333',
        marginBottom: 4,
        letterSpacing: 0.5,
    },
    input: {
        borderWidth: 1,
        borderColor: '#007680',
        borderRadius: 6,
        paddingHorizontal: 12,
        paddingVertical: 10,
        fontSize: 14,
        color: '#333',
    }
});